{
  "scripts": {
    "install:atlas": "npm install --prefix atlas",
    "dev": "npm run dev --prefix atlas",
    "build": "npm run build --prefix atlas",
    "start": "npm run start --prefix atlas"
  }
}
